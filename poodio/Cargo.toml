[package]
edition.workspace = true
name = "poodio"
repository.workspace = true
version.workspace = true

[package.metadata.binstall]
pkg-fmt = "bin"
pkg-url = "{ repo }/releases/download/{ name }%40v{ version }/{ name }-{ target }"

[lib]
crate-type = ["rlib"]

[[bin]]
name = "poodio"
path = "src/main.rs"

[dependencies]
