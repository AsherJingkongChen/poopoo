#! /usr/bin/env node

const loader = require("./loader.cjs");
const { bin, name, version } = require("../../package.json");

const pkgName = loader.buildPkgName(name);
loader.tryInstallPkg(pkgName, version);

const pkgBin = require.resolve(`${pkgName}/${bin}`);
const args = process.argv.slice(2);

require("node:child_process").execFileSync(pkgBin, args, { stdio: "inherit" });
