#! /usr/bin/env node

const { bin, name } = require("../../package.json");
const pkgName = require("./loader.cjs").pkgName(name);
require("node:child_process").execFileSync(
    require.resolve(`${pkgName}/${bin}`),
    process.argv.slice(2),
    { stdio: "inherit" },
);
